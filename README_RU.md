# sentry-mcp (GlitchTip Edition)

MCP сервис от Sentry, адаптированный под GlitchTip, предназначен для AI-агентов, помогающих в написании кода. Наши инструменты и приоритеты сфокусированы на рабочих процессах разработчиков и отладке.

Этот удаленный MCP сервер выступает в роли связующего звена (middleware) с API Sentry/GlitchTip, оптимизированного для кодинг-ассистентов вроде Cursor, Claude Code и других инструментов разработки.

## Совместимость с GlitchTip

Этот MCP сервер был полностью адаптирован и проверен для работы с [GlitchTip](https://glitchtip.com/) — open-source платформой отслеживания ошибок, совместимой с Sentry.

### Доступность Инструментов

| Категория | Инструменты | Описание |
|-----------|-------------|----------|
| **🤝 Общие** | `whoami`, `find_organizations`, `find_projects`, `find_teams`, `find_releases`, `search_issues`, `get_issue_details`, `search_events`, `get_event_attachment`, `get_trace_details`, `update_issue`, `create_team`, `create_project`, `create_dsn` | Стандартные инструменты Sentry, которые отлично работают с GlitchTip. |
| **🚀 Эксклюзивные** | `list_monitors`, `create_monitor`, `list_status_pages`, `create_status_page`, `list_alerts`, `create_alert` | Инструменты, созданные специально для функций GlitchTip (Uptime, Статус-страницы, Алерты). |
| **⚠️ Ограниченные** | `analyze_issue_with_seer`, `search_docs`, `get_doc` | `analyze_issue_with_seer` использует проприетарный AI от Sentry и недоступен в GlitchTip. Инструменты документации ищут по базе знаний Sentry (что полезно для SDK), но не специфично для вашего инстанса GlitchTip. |

Документация GlitchTip API: https://app.glitchtip.com/api/docs

## Начало работы (Quick Start)

### Stdio (Локальное использование)

Для подключения к self-hosted инсталляции Sentry или GlitchTip, используйте `stdio` транспорт.

**Примечание:** Инструменты поиска с AI (`search_events` и `search_issues`) требуют ключ `OPENAI_API_KEY`. Они используют NLP для перевода запросов в синтаксис Sentry. Без ключа эти конкретные инструменты работать не будут, но остальные останутся доступны.

Вам понадобится [Auth Token](https://gitlab.com/glitchtip/glitchtip-backend/-/blob/master/glitchtip/api/permissions.py) с правами:
- `org:read`, `org:write`
- `project:read`, `project:write`
- `team:read`, `team:write`
- `event:read`, `event:write`

Запуск сервера:

```shell
# Пример запуска через npx (если пакет опубликован)
npx @sentry/mcp-server@latest --access-token=ваш-токен --host=glitchtip.your-domain.com
```

Или через переменные окружения:

```shell
SENTRY_ACCESS_TOKEN=ваш_токен_длинная_строка
SENTRY_HOST=glitchtip.your-domain.com
OPENAI_API_KEY=sk-... # Опционально, для AI-поиска
```

Если переменная `SENTRY_HOST` не задана, CLI автоматически попытается подключиться к облачному `sentry.io`. Для GlitchTip **обязательно** указывайте хост.

### Настройка клиентов (Cursor, Claude)

Чтобы использовать этот сервер в Claude Desktop или Cursor, добавьте следующую конфигурацию в ваш файл настроек (например, `~/Library/Application Support/Claude/claude_desktop_config.json` или `.mcp.json` в корне проекта).

**Для GlitchTip (Из исходников):**

```json
{
  "mcpServers": {
    "glitchtip": {
      "command": "node",
      "args": [
        "/absolute/path/to/sentry-GlitchTip-mcp/packages/mcp-server/dist/index.js"
      ],
      "env": {
        "SENTRY_ACCESS_TOKEN": "your-glitchtip-token",
        "SENTRY_HOST": "glitchtip.your-domain.com",
        "OPENAI_API_KEY": "sk-..."
      }
    }
  }
}
```

**Для GlitchTip (NPX - Рекомендуется):**

```json
{
  "mcpServers": {
    "glitchtip": {
      "command": "npx",
      "args": [
        "-y",
        "@sentry/mcp-server@latest",
        "--access-token=your-glitchtip-token",
        "--host=glitchtip.your-domain.com"
      ],
      "env": {
         "OPENAI_API_KEY": "sk-..."
      }
    }
  }
}
```

### MCP Inspector

В репозитории есть встроенный Inspector для тестирования:

```shell
pnpm inspector
```

## Локальная Разработка

Чтобы внести вклад или изменить код:

1.  **Настройка окружения:**
    ```shell
    make setup-env
    ```

2.  **Настройка учетных данных:**
    Отредактируйте `.env` в корне проекта:
    ```bash
    SENTRY_ACCESS_TOKEN=...
    SENTRY_HOST=...
    OPENAI_API_KEY=...
    ```

3.  **Сборка и запуск:**
    ```shell
    pnpm install
    pnpm build
    pnpm dev
    ```

### Тестирование

*   **Unit тесты:** `pnpm test`
*   **Ручное тестирование (CLI):**
    ```shell
    pnpm -w run cli "who am I?"
    ```

## Примечания для разработчиков

Этот репозиторий использует автоматизированные инструменты проверки кода.
Смотрите `CLAUDE.md` и `AGENTS.md` для получения полной карты документации и рабочих процессов.
